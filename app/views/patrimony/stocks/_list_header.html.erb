<tr>
  <% (resource_class.list_attributes - ['principal']).each do |attribute| %>
    <th><%= localized_attribute(resource_class, attribute) %></th>
  <% end %>

  <th width="122px" align="center">
    <%= localized_attribute(resource_class, :principal) %>
  </th>

  <th width="122px"></th>
</tr>
