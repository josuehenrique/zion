// Functions TO NOT USE on TEST environment
function setDatePicker() {
  $('.date-picker').datepicker({
    format: 'dd/mm/yyyy',
    language: 'pt-BR',
    autoclose: true,
    todayHighlight: true
  });
}
